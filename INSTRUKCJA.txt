================================================================================
INSTRUKCJA URUCHOMIENIA LOKALNIE
================================================================================

WYMAGANIA:
----------
- Python 3.8+
- PyTorch
- torchvision
- matplotlib
- Pillow
- opencv-python   (odszumianie)
- pdf2image       (obsługa plików PDF)
- numpy

INSTALACJA ZALEŻNOŚCI:
----------------------
Otwórz terminal/cmd i wpisz:

    pip install torch torchvision matplotlib pillow opencv-python pdf2image numpy

Lub jeśli masz GPU NVIDIA (szybsze trenowanie):

    pip install torch torchvision --index-url https://download.pytorch.org/whl/cu118
    pip install matplotlib pillow opencv-python pdf2image numpy


================================================================================
JAK URUCHOMIĆ
================================================================================

KROK 1: Otwórz terminal w folderze projektu
-------------------------------------------
    cd C:\Users\User\Desktop\OCR_ZPI


KROK 2: Pobranie i przygotowanie danych
----------------------------------------
    python run_local.py --prepare

    To pobierze dataset Chars74K (~25 MB) i rozpakuje go.


KROK 3: Wytrenowanie modelu
----------------------------
    python run_local.py --train --epochs 10

    Trenowanie na CPU może potrwać. Na GPU będzie szybciej.
    Model zostanie zapisany do pliku: model_ocr.pth

    Dodatkowe opcje trenowania:
    python run_local.py --train --epochs 20 --batch-size 64
    python run_local.py --train --resume checkpoint.pth      (wznowienie treningu)


KROK 3b: Nieskończony trening (opcjonalnie)
--------------------------------------------
    python run_local.py --infinite

    Trening będzie kontynuowany do momentu przerwania (Ctrl+C).
    Checkpointy zapisywane są automatycznie co 5 epok (domyślnie).

    Przykłady:
    python run_local.py --infinite --checkpoint-interval 10
    python run_local.py --infinite --batch-size 64 --resume checkpoint.pth


KROK 4: Rozpoznanie własnego zdjęcia
--------------------------------------
    python run_local.py --image sciezka/do/zdjecia.png

    Przykłady:
    python run_local.py --image C:\Users\User\Desktop\litera_A.png
    python run_local.py --image ./test_images/znak.jpg
    python run_local.py -i moje_zdjecie.png


================================================================================
ODSZUMIANIE (opcjonalne)
================================================================================

Włączenie odszumiania:
----------------------
Dodaj flagę:
    --denoise

Wybór metody:
-------------
    --denoise-method {nlm-color, median, bilateral, gaussian}
    (domyślnie: nlm-color)

Dodatkowe parametry:
--------------------
    --nlm-h       (int, domyślnie 10)     siła NLM dla luminancji (nlm-color)
    --nlm-h-color (int, domyślnie 10)     siła NLM dla kolorów (nlm-color)
    --ksize       (int, domyślnie 3)      rozmiar jądra dla median/gaussian (3,5,7,...)

Przykłady użycia:
-----------------
1) NLM dla koloru (polecane jako start):
    python run_local.py --image test_A.png --denoise --denoise-method nlm-color --nlm-h 12 --nlm-h-color 12

2) Median (gdy masz szum „sól i pieprz"):
    python run_local.py --image test_A.png --denoise --denoise-method median --ksize 5

3) Bilateral (dobry kompromis: odszumia i zachowuje krawędzie):
    python run_local.py --image test_A.png --denoise --denoise-method bilateral

4) Gaussian (delikatne wygładzenie):
    python run_local.py --image test_A.png --denoise --denoise-method gaussian --ksize 5

Uwaga:
------
- Odszumianie działa także z trybami:
    --word  (rozpoznanie wyrazu)
    --lines (rozpoznanie linii/tekstu)
  Wystarczy dodać flagi jak wyżej, np.:
    python run_local.py --word napis.png --denoise --denoise-method nlm-color

- Podgląd obrazu w wynikowym oknie (wizualizacja) pokazuje obraz po odszumianiu,
  jeśli --denoise było włączone.


================================================================================
WSZYSTKIE OPCJE
================================================================================

    python run_local.py --help

  TRYBY DZIAŁANIA (wybierz jeden):
    --prepare, -p          Tylko pobierz i przygotuj dane
    --train, -t            Trenuj model przez określoną liczbę epok
    --infinite             Nieskończony trening (zatrzymaj przez Ctrl+C)
    --image, -i   PLIK     Rozpoznaj pojedynczą literę
    --word, -w    PLIK     Rozpoznaj wyraz (jedna linia tekstu)
    --lines, -l   PLIK     Rozpoznaj tekst wieloliniowy
    --multi, -m   PLIK...  Rozpoznaj wiele zdjęć pojedynczych liter

  PARAMETRY TRENOWANIA:
    --epochs, -e           Liczba epok trenowania (domyślnie 10)
    --batch-size, -b       Rozmiar batcha (domyślnie 32)
    --checkpoint-interval  Interwał zapisu checkpointów w trybie infinite (domyślnie 5)
    --resume, -r  PLIK     Wznów trening z pliku checkpointu (.pth)

  ODSZUMIANIE OBRAZU:
    --denoise              Włącz odszumianie wejścia
    --denoise-method       Metoda: nlm-color | median | bilateral | gaussian
    --nlm-h                Siła NLM – luminancja (domyślnie 10)
    --nlm-h-color          Siła NLM – kolor (domyślnie 10)
    --ksize                Rozmiar jądra (median/gaussian): 3, 5, 7, ...

  FORMAT WYJŚCIA:
    --output, -o  PLIK     Ścieżka do pliku wyjściowego (txt/json)
    --output-format, -f    Format: console (domyślnie) | txt | json
    --quiet, -q            Cichy tryb – wyłącz wyświetlanie na konsoli

  PRZYKŁADY UŻYCIA:
    python run_local.py --image litera.png -f json      Zapis do folderu wynik/ jako JSON
    python run_local.py --word wyraz.png -f txt         Zapis do folderu wynik/ jako TXT
    python run_local.py --image litera.png -o moj_wynik.json   Własna ścieżka pliku
    python run_local.py --word wyraz.png -f json -q     Cichy tryb (tylko wynik)

Uwaga: opcje --prepare / --train / --infinite / --image / --word / --lines / --multi
       wykluczają się wzajemnie (tylko jedna na raz).


================================================================================
JAK DODAĆ WŁASNE ZDJĘCIE
================================================================================

1. PRZYGOTOWANIE ZDJĘCIA:
   - Zdjęcie powinno zawierać JEDEN znak (literę) dla trybu --image
   - Najlepiej na jasnym/białym tle
   - Formaty: PNG, JPG, JPEG, BMP, PDF
   - Dowolny rozmiar (zostanie przeskalowane do 48x48)

2. PRZYKŁADOWE ZDJĘCIA TESTOWE:
   Możesz stworzyć własne zdjęcia w programie Paint:
   - Otwórz Paint
   - Ustaw rozmiar obrazu np. 100x100 pikseli
   - Napisz jedną literę
   - Zapisz jako PNG

3. URUCHOMIENIE:
   python run_local.py --image nazwa_pliku.png


================================================================================
STRUKTURA PLIKÓW
================================================================================

OCR_ZPI/
├── run_local.py                   <- Główny skrypt uruchomieniowy
├── ocr/
│   ├── __init__.py
│   ├── main.py                    <- Punkt wejścia (logika CLI)
│   ├── config.py                  <- Stałe konfiguracyjne (ścieżki, rozmiary, klasy)
│   ├── model.py                   <- Architektura sieci CNN (SimpleCNN)
│   ├── trainer.py                 <- Pobieranie danych i trenowanie modelu
│   ├── inference.py               <- Rozpoznawanie liter / wyrazów / tekstu
│   ├── display.py                 <- Wizualizacja i wyświetlanie wyników
│   ├── output.py                  <- Obsługa formatu wyjścia (console/txt/json)
│   └── utils.py                   <- Narzędzia: transformacje, odszumianie, I/O
├── chars74k_ocr_preparation.py    <- Przygotowanie danych (wersja Colab)
├── split.py                       <- Segmentacja liter z obrazu alfabetu
├── instrukcja.txt                 <- Ta instrukcja
├── model_ocr.pth                  <- Wytrenowany model (po trenowaniu)
├── prediction_result.png          <- Wynik wizualizacji (po predykcji)
├── YYYY-MM-DD/                    <- Foldery z datą – historia rozpoznanych zdjęć
└── data/                          <- Dane (tworzone automatycznie)
    ├── EnglishFnt.tgz             <- Pobrany dataset
    └── English/
        └── Fnt/                   <- Rozpakowane obrazy znaków


================================================================================
PRZYKŁAD SESJI
================================================================================

C:\Users\User\Desktop\OCR_ZPI> python run_local.py --prepare
Używane urządzenie: cpu
Pobieranie datasetu z http://www.ee.surrey.ac.uk/CVSSP/demos/chars74k/EnglishFnt.tgz...
Postęp: 100.0%
Pobrano!
Rozpakowywanie...
Rozpakowano!
Dane przygotowane!

C:\Users\User\Desktop\OCR_ZPI> python run_local.py --train --epochs 5
Używane urządzenie: cpu
============================================================
TRENOWANIE MODELU
============================================================
Urządzenie: cpu
Zbiór treningowy:  5765 obrazów
Zbiór walidacyjny: 1442 obrazów
Liczba klas:       62
Epoch [1/5]  Step [50/181]  Loss: 2.3456
...
Trenowanie zakończone! Najlepsza dokładność: 85.23%
Model zapisany do: ./model_ocr.pth

C:\Users\User\Desktop\OCR_ZPI> python run_local.py --infinite
Używane urządzenie: cpu
Uruchamianie nieskończonego treningu...
Naciśnij Ctrl+C aby wstrzymać i wyświetlić menu opcji.
Epoch [1/∞]  Step [50/181]  Loss: 2.1234
...
(Ctrl+C aby przerwać)

C:\Users\User\Desktop\OCR_ZPI> python run_local.py --image test_A.png
Używane urządzenie: cpu
Rozpoznawanie: test_A.png
Zapisano zdjęcie jako: ./2026-03-17/1.png
Wczytywanie modelu z ./model_ocr.pth...
Model wczytany!

========================================
WYNIK: 'A' (pewność: 94.5%)
========================================

Top 5 predykcji:
  1. 'A' – 94.5%
  2. 'H' – 2.1%
  3. 'a' – 1.3%
  4. 'R' – 0.8%
  5. '4' – 0.5%

Zapisano wizualizację do: prediction_result.png

C:\Users\User\Desktop\OCR_ZPI> python run_local.py --lines tekst.png
Używane urządzenie: cpu
Rozpoznawanie tekstu: tekst.png
Wczytywanie modelu z ./model_ocr.pth...
Model wczytany!

Rozpoznany tekst:
Hello World
OCR dziala


================================================================================
ROZWIĄZYWANIE PROBLEMÓW
================================================================================

PROBLEM: "ModuleNotFoundError: No module named 'torch'"
ROZWIĄZANIE: pip install torch torchvision

PROBLEM: "ModuleNotFoundError: No module named 'cv2'"
ROZWIĄZANIE: pip install opencv-python

PROBLEM: "ModuleNotFoundError: No module named 'pdf2image'"
ROZWIĄZANIE: pip install pdf2image

PROBLEM: "No such file or directory"
ROZWIĄZANIE: Upewnij się, że jesteś w folderze OCR_ZPI
             cd C:\Users\User\Desktop\OCR_ZPI

PROBLEM: "UWAGA: Nie znaleziono modelu model_ocr.pth"
ROZWIĄZANIE: Najpierw wytrenuj model: python main.py --train

PROBLEM: Rozpoznawanie słabe/niepoprawne
ROZWIĄZANIE:
- Upewnij się, że zdjęcie zawiera tylko jeden znak
- Znak powinien być czytelny i na kontrastowym tle
- Wytrenuj model dłużej: python run_local.py --train --epochs 20
- Użyj nieskończonego treningu: python run_local.py --infinite
- Spróbuj odszumiania: python run_local.py --image plik.png --denoise

================================================================================
